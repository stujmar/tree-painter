function findColors(rgbaStart, rgbaEnd, timeStart, timeEnd){
    // add logic to transistion color.
    // return single rgba value to plug into svgs/divs.
    return [rgbaStart, rgbaEnd, timeStart, timeEnd]
}

export { findColors }